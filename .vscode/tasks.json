{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "shell",
      "command": "cd cli && yarn install && yarn build",
      "problemMatcher": [],
      "label": "npm: build - cli",
      "detail": "Install dependencies and build CLI before debugging"
    }
  ]
}
